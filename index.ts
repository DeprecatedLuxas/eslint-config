// File used to test different eslint config properties with.

class TestClass {
  constructor(private readonly test: string) {
    this.test = test;
  }
}

enum TestEnum {
  Test = "test",
  Test2 = "test2",
}
